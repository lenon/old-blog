#page
  %h1.page-title Dashboard
  .section
    %h2.section-title Posts
    -Post.all.each do |post|
      %a{ :href => "/admin/edit-post/#{post.id}" } #{post.title}
    .footer
      %a{ :href => "/admin/new-post" } New post
