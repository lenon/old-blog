!!!
%html
  %head
    %meta{ :charset => "utf-8" }
    %title Lenon Marcel #{if @title.nil? then ' | Blog' else ' | ' + @title end}
  %body
    #container{ :dir => "ltr" }
      %header#header
        %h1 Lenon Marcel
        %h2 forever programmer
      #main{ :role => "main" }
        =yield
      %footer &copy #{Time.now.year} Lenon Marcel
