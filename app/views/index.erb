<% @posts.each do |post| %>
<div id="post-<%= post.id %>" class="post home">
  <div class="post-meta">
    <%= l post.created_at, :full %>
  </div>
  <h2 class="post-title">
    <a href="<%= url "/post/#{post.slug}" %>"><%= h post.title %></a>
  </h2>
</div>
<% end %>
